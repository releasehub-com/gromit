# frozen_string_literal: true

source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in gromit.gemspec.
gemspec

group :development do
  gem "pry"
  gem "pry-byebug"
  gem "rake"
  gem "rspec"
  gem "rspec-expectations"
end
